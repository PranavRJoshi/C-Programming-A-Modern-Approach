#include <stdio.h>

int main (void) {
    printf("Parkinson's Law:\nWork expands so as to ");
    printf("fill the time\n");
    printf("available for its completion. \n");
    return 0;
}

// Q. Identify the directives and statements in this program.
// A. One directive: `#include <stdio.h>`, and four statements: three printf statement and one return statement
// Q. What output does the program produce?
// A. Expected Output:
// Parkinson's Law:
// Work expands so as to fill the time
// available for its completion.
// A. Actual Output: [Same as the one expected]
